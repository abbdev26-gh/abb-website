# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Deployment build artifacts — must NOT be committed. These contain
# platform-native binaries (better-sqlite3, sharp) built for the local OS;
# committing them and deploying prebuilt breaks Vercel's Linux runtime.
.vercel
.netlify

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example
